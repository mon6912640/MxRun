# MxRun 开发进度与恢复指引

> 最后更新：2026-09-13
> 用途：**下次开工先读这一页**。记录了已验证的事实、未验证的假设、待决策项和下一步顺序。

---

## 0. 一句话状态

路线（Rust + egui 自绘）已用实测锁死，启动期地基已固化，AltRun 的三条核心手感已对齐。
**P0-1/P0-2/P0-3 全部完成并实测**：新命令模型 + 执行器 + 导入器。`mxrun.exe --import` 已能用真实文件导入 60 条命令（实测 `lines=76 commands=72 imported=60 needs_input=5 builtin=4`）。
**下一步只差"把导入跑在你自己的数据目录上"**——跑完 MxRun 就第一次是你的启动器了。之后是 P0-4 应用自动发现。

---

## 1. 提交历史

> **2026-09-14：历史已重置。** 首次推送到公开仓库后发现提交历史里含本机路径、用户名与软件清单，
> 按"脱敏 + 重写历史"处理：**仓库现在只有一个初始提交**，此前那些提交（含他人代码 `ALTRun.ahk`）
> 已从本地与远程移除。哈希列因此作废，下面按当时顺序保留**工作日志**：
>
> | 内容 |
> |---|
> | MVP v0.1 全量首次提交（9 文件 10314 行）+ 单实例与启动失败可见化 |
> | README 补记单实例踩坑 |
> | 首帧句柄查找修复；渲染后端 wgpu → glow |
> | DWM 系统材质可行性验证（Acrylic 可达、Mica 不适合） |
> | 真实系统图标（`IShellItemImageFactory` + 预热 + emoji 回退）+ 首批单元测试 |
> | AltRun 三条核心手感（空格执行 / 失焦隐藏 / Esc 二级） |
> | README 补记三条手感与空格取舍 |
> | 开发进度与恢复指引 + CLAUDE.md |
> | 查清 AltRun 右键加项机制（SendTo 活 / shell 菜单是死代码）+ SendTo 完整交互链路 |
> | P0-1 命令模型落地（条目/动作/来源）+ 安全迁移与触发词搜索 |
> | P0-2 执行器（五种效果 + 参数机制 + 需要输入标记） |
> | 两条真动系统的端到端测试（真实窗口隐藏/恢复、剪贴板往返） |
> | 「恢复窗口」改为恢复上一次隐藏的那个窗口，而非当前快照 |
> | 自检开关 `MXRUN_SELFTEST`（无键盘验收"搜索→列表→执行"链路） |
> | P0-3 导入器（`--import` / `--import-all`）+ 搜索分数闸门 |
> | 跳过用户不要的 `cmd` 条目（Dos窗口，`cmd /k %p`） |
> | 导入真实数据后自动清掉演示条目（`seed` + `legacy` 两种来源） |
> | 列表行显示启动次数 + 补全空白的内容行 |
> | 图标改成按需 + 后台 + 有上限缓存（冷启动 850ms → 255ms） |
>
> **防复发**：`.githooks/pre-commit` 会扫描暂存内容，命中个人信息直接拒绝提交
> （拦截词在 `local/personal-patterns.txt`，两者都不进仓库）。clone 后执行一次
> `git config core.hooksPath .githooks`。

---

## 2. 已验证的事实（都有实测证据，别再重新怀疑）

### 2.1 性能（README「性能实测」节有完整表格）

| 指标 | 实测 | 备注 |
|---|---|---|
| 热键按下 → 首帧渲染 | **1.2–5.9 ms**（p50 ~3.7ms） | 设计预算 50ms，余量一个数量级；59 条数据下复测仍是 **2.5–3.9 ms** ✅ |
| 私有提交（常驻，与显隐无关） | **~106–157 MB** | 真实占用看这个 |
| 工作集（显示中） | ~130–270 MB | 含 GPU 映射 |
| 工作集（隐藏数小时后） | ~11 MB | **只是系统 trim 的结果，不是真实占用** |
| 线程 | 27 | glow 后端 |
| 冷启动 → 首帧 | **~255–295 ms**（59 条数据） | 拆解：eframe/GL/字体初始化占掉绝大部分，**我们自己的数据只花 0 ms** |
| 冷启动 → 图标齐 | **~430–450 ms** | 首帧后 ~175 ms 内补齐可见行的图标（emoji 先顶上） |

**图标加载在 2026-09-14 重做过（原来是冷启动最大的开销）**：

- 老做法：启动时**阻塞预热全部**图标，再显示窗口。实测 **6 条 49–67 ms → 59 条 544–770 ms**（线性增长）。也就是冷启动约 `250 ms(框架) + 600 ms(图标) ≈ 850 ms`，而且**条目越多越慢**——正是"后面越来越多会卡顿"的那条路。
- 新做法：**按需 + 后台 + 有上限缓存**（`Icons`）。索引建完就出窗口（0 ms），行渲染时才请求自己那一行的图标，worker 线程取像素，UI 帧里上传纹理；缓存上限 256（顺带修掉"缓存无上限"那条欠账，对将来的文件搜索很关键）。
- 附带：`main` 里还起了个短命线程，在 eframe 初始化期间**提前把 shell 映像子系统预热掉**（首次 `IShellItemImageFactory` 调用要 ~145 ms，实测降到 ~122 ms、图标就绪提前约 40 ms）。
- 实测时间线（3 次冷启动）：`first frame 251/255/260 ms` → `worker first fetch 122–124 ms` → `全部可见图标 ~440–450 ms`。

**渲染后端 = glow(OpenGL)**，不是 eframe 默认。对比 wgpu：提交 464MB / 55 线程 / 881 句柄，画面逐像素一致。`MXRUN_RENDERER=wgpu` 是退路。

### 2.2 系统材质（E2）

- **Acrylic 可达**：`DwmSetWindowAttribute(hwnd, DWMWA_SYSTEMBACKDROP_TYPE, 3)` 在 egui/glow 的透明无边框置顶窗口上工作正常，能把背后内容（含其他窗口）糊成柔和色块。不需要换 WinUI。
- **Mica 不适合做启动器材质**：`DWMSBT_MAINWINDOW` 只采样壁纸、刻意不显示背后窗口，深色壁纸区域表现为死平暗色（实测像素恒定 `30,32,37`）。
- 开关（默认 off）：`MXRUN_BACKDROP=acrylic|mica|off|<n>`、`MXRUN_CARD_ALPHA=0-255`。
- **待处理**：材质会填满整个窗口矩形，现在"靠透明角做圆角"的做法会失效，圆角要改用 `DWMWA_WINDOW_CORNER_PREFERENCE`。

### 2.3 图标（E3）

- `IShellItemImageFactory` 取图，按 `24 * pixels_per_point` 请求像素、按 24pt 绘制。
- `cargo test` 实测覆盖：**exe(绝对路径) / exe(PATH 裸名) / 目录 / .bat / .lnk** 五类全部取到 48×48 真实像素；URL 与不存在的路径返回 None 走 emoji 回退。
- 索引构建时**一次性预热**，搜索热路径只查 HashMap。
- 遗留：URL 没有文件图标（要不要做 favicon 未定）。

### 2.4 启动期加固

- 单实例：`CreateMutexW("MxRun.SingleInstance.v1")`，重复启动弹提示后干净退出。
  **可升级**：AltRun 的做法是给已有窗口发自定义消息让它显示、自己退出（规格 §11），比弹框更顺。
- 启动失败（数据库/托盘/热键初始化）：弹 Win32 对话框说明原因 + 退出码 1，不再 `.expect()` 裸崩。
- 首帧句柄查找：**不要**用 `IsWindowVisible` 过滤——winit 首帧还没显示窗口，会导致启动不居中（已修）。

### 2.5 P0-1 命令模型落地（2026-09-13 完成并实测）

- **新模型**（定义在 `src/store.rs`，设计见 `命令模型设计.md` §4）：
  - 条目 = 标题 / 副标题 / **触发词（多值）** / **动作列表** / 参数规格 / 启动方式 / **来源** / 健康
  - 动作效果是**封闭集合**：打开 / 运行 / 内建 / 复制 / 定位（加能力就加动词，不加类型）
  - 参数规格 = **来源 + 编码 + 插入方式**三件独立的事（不再是一个枚举兼职四职）
  - 来源 = `provider + external_id`（导入幂等、识别"源里删了"、多来源互不干扰）
- **迁移安全**（三条一起做）：
  1. 所有结构 `#[serde(default)]` → 老记录仍能反序列化
  2. 迁移由 meta 的 **`schema_version`** 驱动（缺失 = v1）；迁移**只在版本落后时**跑（因为 v1 记录在加了默认值后会"成功解析成空条目"，这是个陷阱）
  3. **坏记录不再静默丢弃** → 写进 `Store::warnings`，再由 `main` 写 `mxrun.log`；**id 保持不变**（frecency 按 id 存，变了统计就脱钩）
- **实测验证**（用**真实库的副本** + 独立 `APPDATA` 跑，不动真库）：
  - 首次启动：`store: migrate: v1 -> v2, 6 条已转换`
  - 图标 `icons: 4/6 resolved` —— 4 条 exe 取到真图标、2 条 URL 走 emoji 回退，**这正好证明动作目标迁移正确**（图标是从动作里推导的）
  - 二次启动：**不再迁移**（幂等），图标仍 4/6（数据完好）
  - `backups/` 每次启动留一份，实测 2 份
- **搜索**：触发词进入搜索池并**加权**（精确 1000 / 前缀 400 / 包含 200）
  - 修掉了 v1 的老毛病：索引只拼 `name + desc`，关键字（`myip` 这种）**永远搜不到**
- **测试**：`cargo test` **12 项全过**（含"关键词命中压过模糊标题命中""只靠关键词也能搜到""三级权重顺序""旧库迁移保留 id""坏记录被上报""重复导入不产生重复"）
- **执行**：暂留**桥接**——只有「打开/运行」接到原来的 `open::that`，内建/复制/定位会提示"暂未实现"。**真执行器是 P0-2**（参数插入、启动方式、内建动词）。
- 顺带清掉的死代码：`kind_icon`/`kind_label`（9 分类没了）；`add_command`/`delete_command`/`export_json` 改名为 `upsert_item`/`delete_item`/`export_json` 并标注"给 CRUD/导入预留"。

### 2.6 P0-2 执行器（2026-09-13 完成并实测）

新增 `src/exec.rs`，`run(item, action)` 分派五种效果：

| 效果 | 调用 | 为什么是它 |
|---|---|---|
| `Open` | `ShellExecuteW` | 只有它能认文档/目录/网址/协议/CLSID/`.msc`/`.cpl`——这些都不是可执行文件 |
| `Run` | `CreateProcessW` | **带参数的命令行不是一个文件名**。AltRun 把整串交给 `ShellExecute`，只因后面那个 `WinExec` 兜底才没露馅（`untShortCutMan.pas:341-350`）；按效果拆开就没有这个陷阱 |
| `Builtin` | 进程内 Win32 | 显示桌面 = 给 `Shell_TrayWnd` 发 `MIN_ALL`(419)；隐藏/恢复 = `ShowWindow`；关机/重启 = `shutdown /s|/r /t 5`。**不引入外部 exe**。⚠️ **恢复窗口必须用"上一次被隐藏的那个句柄"**，不能用当前快照——等用户再次呼出时，前台窗口早已换成他刚点的那个了（AltRun 的 `WinCtl.exe UnHide` 不带参数也是这个原因） |
| `Reveal` | `explorer.exe /select,"…"` | |
| `Copy` | 剪贴板 | |

**参数机制落地**（`ArgSpec` 那三件事）：

- **编码**：原样 / URL / UTF-8。后两者在 Rust 里都是 UTF-8 百分号编码——AltRun 的差别只源于 Delphi 从 ANSI 字节编，现代化后两者等价（数据字段保留，行为统一）
- **插入**：`Replace` 会填 `{p}` / `%p` / `{%c}` 中出现的任何一种（AltRun 只替换**第一个**）；`Append` 追加到末尾——**搜索引擎就是靠它**
- **来源**：剪贴板、前台窗口（句柄/标题/类名）今天可用；**`Prompt` 没有界面 → 返回 `NeedsInput`**
- **前台窗口快照必须在呼出前取**（`show_window` 里调 `remember_foreground`）。否则"前台窗口"就是 MxRun 自己——AltRun 也是在热键按下瞬间取的值（规格 §4）

**`*` 标记（用户选的 C 方案）**：需要输入参数的条目，副标题显示「需要输入 *」；按回车不执行，而是提示"输入功能在下一步加入"。这样导入后不会让人以为程序坏了。

**顺带修掉两个图标缺口**（样本档案一跑就暴露，导入数据里全是这类）：

1. `%WINDIR%` / `%PROGRAMFILES%` 这类**环境变量目标**：图标解析前先展开
2. **裸名与带参数命令**：按 `PATHEXT` 在 PATH 里补扩展名解析——`nslookup`、`cmd /k %p`、`shutdown /s /t 5` 以前都取不到图标

实测：样本档案图标 **4/13 → 7/13**（剩下 3 条网址 + 2 条内建动作本来就没有文件图标）。

**测试**：`cargo test` **25 项**（新增 13 项）。其中两项是**真的动了系统**的端到端测试：

- **真实窗口的隐藏/恢复**：真起 notepad → 按应用的方式快照前台窗口 → 用执行器自己的动词隐藏 → 用 Win32 `IsWindowVisible` 校验 → 恢复 → 杀掉。**而且它是安全的**：如果前台窗口不是 notepad（比如是你的终端），它会**跳过而不是失败**——绝不会去隐藏别的窗口
- **剪贴板往返**：写进去再读回来，读之前先保存原内容、测完还原；**剪贴板里没有文本时直接跳过**（避免把复制的图片毁掉）

仍未自动化的只有**界面交互**本身（键盘输入进不了 egui，见 CLAUDE.md 踩坑 3）。

**人工验证入口**（键盘交互无法自动化，见 CLAUDE.md 踩坑 3）：

```
cargo test -- --ignored --nocapture seed_profile_for_manual_check
# 生成独立样本档案（7 条覆盖全部执行路径），按它打印的命令启动即可逐条试
```

### 2.7 P0-3 导入器（2026-09-13 完成并实测）

新增 `src/import.rs` + `--import` 开关（GUI 程序没有控制台，所以结果既写 `mxrun.log`，也弹一个汇总框）。

**用真实文件实测**（2026-09-14 已代为导入到真实数据目录 `%APPDATA%\MxRun`）：

```
import: lines=76 commands=72 imported=59 needs_input=4 builtin=4 removed_seed=6 skipped(sep=4 unwanted=13 unsupported=0)
icons: 47/59 resolved
```

**导入会自动清掉演示数据**（`removed_seed=6`）：新库的种子条目 provider 是 `seed`，而 v1 老库迁移出来的是 `legacy`——两种都要删，否则列表里会出现"计算器"和导入的 `Calc` 并存这类重复。判断依据写在 `DEMO_PROVIDERS` 常量里，注释说明了为什么 `legacy` 也一定是演示数据（v1 的 `add_command` 从未被调用过，用户不可能加过自己的命令）。

**踩到的坑（值得记）**：第一版只按 `provider == "seed"` 删，结果真实库 `removed_seed=0`——因为用户那 6 条是老格式迁移来的（`legacy`）。单元测试当时用的是新库（`seed`），没能覆盖这条路径；现在测试会先把 provider 改成 `legacy` 再验。

**两条不照抄的规则**（都在代码里，`--import-all` 可关掉）：

1. **不灌 `F<n>` 权重** —— 那是作者模板里的预置值（§6.1.1），灌进来等于把一个十几年前、不衰减的排序冻进 frecency
2. **`@.\WinCtl.exe …` 映射成内建动词**（显示桌面 / 只显示当前窗口 / 隐藏 / 恢复），并**丢掉那个 `@` 前缀**（它原本只是"别让助手 exe 闪窗口"）

**跳过 13 条**：12 条无用条目（IE、Internet 控制、添加删除程序、系统属性、`%WINDIR%`/System32/ProgramFiles 三个目录、`.\ALTRun.ini`、升级死链、搜狗MP3、VeryCD、Yahoo）+ **1 条用户明确不要的**：`cmd`（Dos窗口，`cmd /k %p` —— 一个先弹框问你"要运行什么"的条目，用户 2026-09-13 确认不会用到）。

**关键实现细节**：

- **编码**：文件是 GBK（Delphi 写的是 ANSI 代码页）。用 Win32 `MultiByteToWideChar(CP_ACP)` 解码，**不引入新依赖**（已在用 `windows` crate）；UTF-8 文件也照样读
- **命令行里含 `|`**：只 `splitn(5, '|')`，第五列保留原样（URL 里可能有 `|`）
- **打开还是运行**：URL/`::`/`shell:`/`ms-settings:` → 打开；**目标后面带参数 → 运行**（`cmd /k %p`、`shutdown /s /t 5`）；只有一个目标 → 打开
- **参数规格从数据推导**：`{%c}`→剪贴板、`{%wd}`→前台窗口、其余非空编码列→需要输入；有占位符→替换，没有→追加（搜索引擎的机制）

**实测查询**（用导入后的档案，自检开关）：

| 查询 | 结果 | 说明 |
|---|---|---|
| `stea` | **1 条**（目标应用） | 关键字前缀命中 |
| `cb` | **1 条**：百度 搜索剪贴板 | ⚠️ `needs_input=false` —— 见下面的 bug |
| `hide` | 2 条：隐藏本窗口 / 恢复窗口 | 两条都是内建动作，都合理 |
| `dos` | 8 条（Dos窗口 排第 1） | 关键字是 `cmd` 不是 `dos`，只能靠标题模糊匹配 → 噪音仍在 |

**顺带修掉的一个真 bug**：`wants_input()` 原本写成"输入来源 ≠ 无"，于是**剪贴板搜索（`cb`/`cg`）也会被标成"需要输入"**——但它们根本不用打字。改成只认 `Prompt`。单元测试当场抓住了它（`search_engines_append_and_clipboard_ones_replace`）。

**同批做的小改进：搜索分数闸门**（`SCORE_GATE = 0.15`）
低于最高分 15% 的结果不再显示。实测效果：`stea` 从一堆噪音变成 1 条、`cb` 1 条、`hide` 2 条。
**但注意**：闸门只在"有关键字命中"时有效（那是一个数量级的差距）；像 `dos` 这种只能靠标题模糊匹配的查询，分数都挤在 70~90 之间，闸门拦不住。要再干净就得动匹配算法（拼音那串长文本里子序列太多），属于后续调优。

**测试**：`cargo test` **37 项**（新增 12 项导入器测试，含一个直接读真实 `ShortCutList.txt` 的集成测试，断言 76 行 / 72 条命令 / 4 分隔线 / 导入 60 / 跳过 12 / 需要输入 5 / 内建 4）。

### 2.8 AltRun 的右键 / 命令行加项通道（2026-09-13 复核）

- **SendTo 是活的**：`%APPDATA%\Microsoft\Windows\SendTo\ALTRun.lnk` 实测存在，指向 AltRun 的 exe（无参数）；`Bin\ALTRun.ini` 里 `[Config] AddToSendTo=1`。这就是用户多年的"右键 → 发送到 → ALTRun"加项习惯。
- **接收端也是活的**：`ALTRun.exe "<路径>"` → 已有实例时 `SendMessage(WM_SETTEXT, 1, 文件名)` 交给它 `AddFileShortCut`；没有实例则自己加载后添加再退出（`frmALTRun.pas:1949-2027`、`:2767-2777`）。最终弹 `frmShortCut` **预填三框**让用户确认（`untShortCutMan.pas:377-408`）。
- **shell 右键菜单是死代码**：`untUtilities.pas:300-333` 的 `AddMeToShortCutMenu`（写 `HKCR\*\shell\Add To ALTRun`）**零调用点**、本机注册表无痕、无安装脚本。MxRun 若要做，注册到 `HKCU\Software\Classes\*\shell`（免管理员），别照抄 `HKCR` 写法。
- 已补进规格：`AltRun交互规格.md` §11 + 高风险清单 13 + 对齐分档 ①。

---

### 2.9 启动次数统计 + 行内容补全（2026-09-14）

用户提出："每个项目的启动次数要统计，并显示在列表项里（现在列表项很空）"。

- **次数来源**：既有的 frecency（`frec:{id}`）。**按 id 存**，而导入器重导时会保留 id（`upsert_from_source`）→ 次数不会被重新导入清掉
- **显示**：列表行**右侧靠右**显示「启动 N 次」；**从未启动显示灰字「未启动」**（不留空、也不堆一排 0）
- **副标题为空时回退显示"它会执行什么"**：导入的条目都没有说明，那一行本来是空的。长目标截断到 64 字符，避免把行撑宽（否则右侧的次数会被挤出去）
- **顺带修掉一个真 bug**：执行后只写了数据库，内存索引里的 `frec_bonus` 和次数还是旧的 → **排序权重和显示要重启才更新**。现在 `refresh_frecency()` 在每次执行后同步
- 实测（用户真实使用记录）：几个常用应用各 1–3 次；空闲列表按次数排序 ✓（具体条目见 `local/机器环境.md`）

## 3. 待办：下一批（**2026-09-13 按证据重排**）
> 排序依据与完整论证见 `docs/命令模型设计.md` §7。核心变化：**F 值被证明是"永久累计、不衰减"的历史值**（§6.5），不再作为优先级依据；活数据显示当前高频是**应用类**，而参数类（运行/搜索）近期没用过。

| # | 工作包 | 内容 | 量级 |
|---|---|---|---|
| ~~1~~ | ~~**模型 + 数据层**~~ | ✅ **已完成（2026-09-13）**，见 §2.5。落地了条目 + 动作列表 + 来源 + 触发词多值 + 参数规格；旧库迁移实测通过；触发词搜索加权已生效。Provider 接口按"查询型"要求设计（为 `@`+es.exe 留缝） | 中 |
| ~~2~~ | ~~**执行器**~~ | ✅ **已完成（2026-09-13）**，见 §2.6。五种效果全部落地（Open/Run/Builtin/Reveal/Copy）；参数按"来源/编码/插入方式"做通，非交互来源可用；`Prompt` 无界面时返回 NeedsInput，列表用 `*` 标记 | 小中 |
| ~~3~~ | ~~**导入器**~~ | ✅ **已完成（2026-09-13）**，见 §2.7。`mxrun.exe --import`（精选，实测导入 59 条）/ `--import-all`（全量 72 条）；GBK 解码、WinCtl 映射成内建动词、不灌 F 权重。**待你跑一次真实导入** | 中 |
| 4 | **应用自动发现**（← 下一步） | 开始菜单 + UWP（`shell:AppsFolder`）→ 条目数最大的一类（34/72 = 47%），做完"新装软件不用手工加" | 中 |

做完这四项，MxRun 才第一次是"用户的启动器"，而且**日常能替换 AltRun**。

**第二批（P1，都卡在同一个总闸门上）**：输入机制最小可用版（`{p}` + 三种编码 + 剪贴板 `{c}`）→ 搜索模板 / 窗口动作内建 / `运行`(Fallback)。**原先把"参数机制"排在第二批第一位，现降为 P1**——它一个人解锁 14 条参数类条目，但当前使用数据不支持它插队。

之后的顺序（用户已认可待办，尚未排定）：

| 工作包 | 内容 | 量级 |
|---|---|---|
| CRUD 管理 | 编辑对话框 + 删除 + 管理列表（`store.upsert_item`/`delete_item`/`export_json` 已就绪并标注了用途） | 中大 |
| 键盘流补全 | 数字键执行第 N 项、序号 + `*` 标记、F2/Insert/Delete/Ctrl+D/Ctrl+C/Ctrl+L、单击选中/双击执行/中键执行、窗口拖动、位置记忆、托盘单击 | 中 |
| **右键集成** | `MxRun.exe "<路径>"` 落库一条命令 + 写 SendTo 快捷方式 + `HKCU\Software\Classes\*\shell` 右键菜单（规范见 `AltRun交互规格.md` §11 与对齐分档 ①）。**不依赖 CRUD 界面**——`store.upsert_item` 已就绪（另见 §3.1 三条设计约束） | 小中 |
| **`@` 文件搜索**（Everything / `es.exe`） | 输入框打 `@` → 调 `es.exe` 搜目录/文件，结果可打开 / 打开所在目录 / 执行。需求、架构要求与坑清单见 `命令模型设计.md` §9。⚠️ **P0-1 的 provider 接口与"多动作"必须为它留缝**，否则以后要拆结果管线重做 | 中 |
| 视觉 | Acrylic 配方 + 圆角 + 明暗主题联动 + 呼出动画 + 排版配色 | 大 |

建议顺序 **CRUD → 键盘流 → 视觉**：键盘流里的 F2 编辑 / Insert 新建必须等编辑界面存在；视觉放最后是因为它会**返工**前面的卡片结构（材质 + 圆角），等交互定型再动最省。
**例外**：右键集成不依赖以上三者，可随时插入（见 §2.8——它是用户现有习惯的加项通道，越早做越快积累真实使用数据）。

**可顺带捎上的小项**：单实例唤醒升级、托盘单击切换显隐。其中"单实例唤醒"与右键集成是**同一套 IPC**（第二实例把路径交给第一实例后自己退出），建议合并做。

### 3.1 右键集成的三条设计约束

从 §2.8 的 AltRun 实测行为反推——实现时别丢：

1. **"不打扰"是这个交互的灵魂**：只弹一个加项小确认框，**主窗口全程不显示**（原版"已运行"和"未运行"两种情况都不显示）。顺手把主窗口拉出来，手感就变了。
2. **确认框是必需的，不是装饰**：自动取的关键字往往不是你想打的那个（`GitHub Desktop.lnk` 自动得 `GitHub Desktop`，你其实想打 `gh`），原版正是靠这个框让你当场改。若不弹框直接落库，就必须先有 CRUD 界面兜底。
3. **两个坑别照抄**：① 关键字或命令行**任一为空**时，原版静默降级成"空行分隔项"——MxRun 应当直接拒绝并留在输入态；② 原版加项路径**绕过单实例保护**（互斥体检查在加项分支之后，两个实例可能同时写列表文件）——MxRun 走 IPC 把路径交给主实例处理，正好避开。

---

## 4. 待用户拍板的决策

1. **`alias` 字段 → 已升级为"命令模型"决策**：不再是"给 `Command` 加一个 alias"，而是按 `docs/命令模型设计.md` 重新分型（应用 / 系统工具 / 路径 / 搜索模板 / 系统动作 / 命令 / 脚本 / 链接 / 分隔线）。**该文档 §8 有 7 条待拍板项**，其中第 1 条（应用要不要自动发现）已被优先级重排（§7）确认为 **P0-4**。
2. **参数输入形态**：AltRun 是弹独立小窗 `frmParam`。**建议改成主窗口内联展开**（交互流程照搬：空格 → 输参数 → 回车 → 执行），理由是不弹子窗口、不打断、与 Acrylic 材质观感一致。**用户尚未表态。**（与 `命令模型设计.md` §8 第 3 条是同一件事；它属于 **P1-1**，不挡 P0）
3. 后三项的顺序（见上表建议）。

---

## 5. 明确不做（已讨论）

- **Fallback 兜底**（`+`/空格/`>` 前缀、Everything/网页搜索）：**Delphi 版根本没有这个机制**，是 AHK 版与调研文档带进来的。用户核心要求是美观 + 性能，不是功能对齐。等真实数据跑起来后再看是否需要。
- **皮肤背景图**（`BG.jpg` + 颜色键）：用 Acrylic 替代（E2 已定论）。
- AltRun 的已知缺陷一律不照搬（拖拽丢 Freq、右键菜单名与动作相反、编辑不改动会被自身判重等，见规格 §11）。

---

## 6. 关键参照

| 文档 / 位置 | 用途 |
|---|---|
| `docs/AltRun交互规格.md` | **对齐的唯一权威**。按键表、热键模型、参数机制、占位符全集、聚焦/隐藏、列表、搜索、排序、窗口行为 + 高风险清单 + **对齐三档（必须补 / 故意不照搬 / 待定）** |
| `docs/命令模型设计.md` | **命令数据模型的权威（2026-09-13 起草）**。72 条真实命令的现代分类（应用 34 / 系统工具 11 / 搜索模板 8 / 路径 6 / 系统动作 6 / 命令 3 / 脚本 3 / 链接 1 / 分隔线 5）+ `param_type` 兼职四职的证据 + 新模型草案 + **明确不照抄清单**。定"命令长什么样"看它，别照抄 AltRun 的字段 |
| `docs/AltRun实现原理调研.md` | AHK 重写版与原始评测的调研（**注意：它描述的多是 AHK 版，与用户实际在用的 Delphi 版有差异**） |
| `docs/现代启动器设计方案.md` | 重构蓝图。**它的路线决策论据（未验证的内存数字）已被实测推翻**，但设计语言与功能规划仍有参考价值 |
| `<AltRun 源码目录>`（本机路径见 `local/机器环境.md`） | 用户日常在用的 **Delphi 原版**源码（GBK 编码，需 `iconv`）+ 真实数据 `Bin\ShortCutList.txt` |
| `docs/ALTRun.ahk` | 另一作者的 AHK 重写版（**不是对齐目标**） |

---

## 7. 踩坑记录（本次新增，避免重复踩）

1. **egui 空闲时不调用 `ui()`** —— 任何轮询式检查（如失焦检测）都必须配 `ctx.request_repaint_after(...)`，否则是死代码。
2. **`MainWindowHandle` 不是可见性探针** —— 它是句柄。判断窗口是否可见要用 Win32 `IsWindowVisible`。
3. **`SendKeys` 只送到前台窗口** —— 从自动化环境启动的 MxRun 拿不到键盘焦点，按键会落到别的窗口（有误伤风险）；`WM_CHAR` 投递也进不了 egui。**自动化测试不能验证键盘交互，必须人工确认**。
4. **运行中的实例会锁住 `target/release/mxrun.exe`** —— 重新构建前先关掉实例，否则 cargo 报 `拒绝访问 (os error 5)`，而 `tail -2` 会把错误吞掉让你以为构建成功了。**构建后务必看一眼 exe 时间戳**。
5. **控制台编码**：PowerShell 输出中文到 Git Bash 会乱码，测试脚本的断言标签用 ASCII。
6. **不要用 `Add-Type -MemberDefinition` + `\"` 转义** —— PowerShell 的转义符是反引号，用 here-string `@"..."@` 更稳。

---

## 8. 环境与操作要点

```bash
export PATH="<MinGW-w64>/bin:$HOME/.cargo/bin:$PATH"   # 真实路径见 local/机器环境.md
cd <项目目录>
cargo build --release     # → target/release/mxrun.exe（构建前先关掉运行中的实例）
cargo test                # 图标管线的单元测试
```

- 数据目录 `%APPDATA%\MxRun\`（`mxrun.redb` + `backups/` + `mxrun.log`）
- **`mxrun.log` 是排查主力**：记录 renderer / 启动 / 图标预热 / `wake:` 呼出延迟 / `hide: lost focus` / `execute:`。写代码时值得继续往里加事件
- 用独立数据目录做实验：`APPDATA="$TEMP/mxrun-test" ./target/release/mxrun.exe`
  （注意：单实例互斥体是**全局**的，不随 APPDATA 变化）
- 温度开关：`MXRUN_RENDERER=wgpu`、`MXRUN_BACKDROP=acrylic`、`MXRUN_CARD_ALPHA=n`
- **自检开关（无键盘验收用）**：`MXRUN_SELFTEST=<查询>` 启动时自动填入关键字、跑**真实**搜索，把每行结果（标题/类别/是否需要输入/得分）写进 `mxrun.log`；再加 `MXRUN_SELFTEST_EXEC=1` 会执行第一行（只对"拒绝执行"的条目安全，比如需要输入的）。
  **这是唯一能在没人按键的情况下验证"搜索 → 列表 → 执行"链路的办法**——`SendKeys` 只会送进当前前台窗口（踩坑 3），自动化环境里会误伤别的窗口。
  实测输出示例（查询 `dos`）：`row0 title="Dos窗口" category=命令 needs_input=true score=1088.0`，随后 `execute: needs input: Dos窗口`。

---

## 9. 下次开工的第一步

1. 先看有没有实例在跑：`tasklist | grep -i mxrun`（或用 PowerShell `Get-Process *mxrun*`）；有就先关掉
   ——**这个坑会咬人**：单实例互斥体是全局的，残留实例会让新实例直接退出并只写一行日志
2. **先把真实数据导进来**（P0-3 的收尾动作，还没跑过）：
   ```powershell
   Get-Process *mxrun* -ErrorAction SilentlyContinue | Stop-Process -Force
   .\target\release\mxrun.exe --import        # 精选 60 条（推荐）；--import-all 是全量 72 条
   ```
   跑完会弹一个汇总框，明细在 `%APPDATA%\MxRun\mxrun.log` 里搜 `import:`
3. 之后的工作包 4（**应用自动发现**）见 §3；P0-1/P0-2/P0-3 已完成（§2.5、§2.6、§2.7）
4. `cargo test` 现在 **37 项**（模型/迁移/搜索权重/编码与插入/真起进程/真实窗口/导入器）。动 store、搜索、exec、import 前后都跑一遍
5. 无键盘验收：`MXRUN_SELFTEST=<查询>`（见 §8）；手工试执行器：`cargo test -- --ignored --nocapture seed_profile_for_manual_check`
6. 搜索噪音还没根治：分数闸门只在"有关键字命中"时有效，靠标题模糊匹配的查询（如 `dos`）仍会带出一屏。要再干净得动匹配算法（拼音串里子序列太多），属后续调优
